apiVersion: v1
kind: Service
metadata:
  name: greengram-like
  namespace: greengram
  labels:
    app: greengram-like
spec:
  selector:
    app: greengram-like
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP   
